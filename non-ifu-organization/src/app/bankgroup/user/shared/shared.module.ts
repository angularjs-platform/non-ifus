const moduleName: string = 'non.ifu.organization.app.bankgroup.user.shared';

// services
import services from './services/services.module';

angular.module(moduleName, [services]);

export default moduleName;
