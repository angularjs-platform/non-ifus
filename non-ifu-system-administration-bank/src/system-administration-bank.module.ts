import * as angular from 'angular';

const moduleName: string = 'non.ifu.system-administration-bank';

import app from './app/app.module';

angular.module(moduleName, [app]);

export default moduleName;
